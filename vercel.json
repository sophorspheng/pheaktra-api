{
  "version": 2,
  "builds": [
    {
      "src": "index.js",  // Adjust the file path if your main file is not named index.js
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "index.js"  // Again, adjust the path if necessary
    }
  ]
}
